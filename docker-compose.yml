services:
  php:
    build: .
    volumes:
      - .:/app
    working_dir: /app

  nginx:
    image: nginx:alpine
    ports:
      - "8088:80"
    volumes:
      - .:/app
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
